server:
  port: 9092

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/api_jpa
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
